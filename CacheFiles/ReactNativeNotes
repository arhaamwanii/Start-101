text component<css text> instead of <p> or h1
it can be syntaxed simillary via css but in a more inside the text component

<text style={{------}}
view component is like dev butadvanced, flecx by default
